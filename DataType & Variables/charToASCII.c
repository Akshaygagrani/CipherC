// Code to convert caharacter to it's ASCII value
// H.W -> Check if it is possible to convert number into its correscponding ASCII caharacter

// 
#include <stdio.h>

int main()
{
    int i;
    i=34;
    
    printf("The value in variable i is: %d", i);
        
    // printf("Give me a caharacter: ");
    // scanf("%c", &chr);
    
    // printf("The caharacter you entered is: %c\n", chr);
    // printf("ASCII value of this caharacter is: %d\n", chr);
    return 0;
}
