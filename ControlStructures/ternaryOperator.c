// Ternary Operator
// Syntax:
//      (condition) ? (trueCodeBlock) : (falseCodeBlock)
#include <stdio.h>

int main()
{
    int m = 5;
    int n = 14;
    
    // if(m>n){
    //     printf("M is greater than N");
    // }else{
    //     printf("N is greater than M");
    // }
    
    (m>n) ? printf("m is greater than n") :  ;

    return 0;
}
